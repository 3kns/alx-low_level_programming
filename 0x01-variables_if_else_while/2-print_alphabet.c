#include <stdio.h>
#include <stdio.lib>
/**
 * main - Entry point with "void" argument
 *
 * Description: "the program's description"
 *
 * Return - Always 0 (Success)
 *
 */
int main(void)
{
	char ch = 'a';

	do {
		putchar(ch);
		ch++
	} while (ch <= 'z');
	break;

	return (0);
}
