#include "main.h"
/**
 * _abs - entry point
 * @int: type of value to be returned
 * Return: absolute value (success)
 */
int _abs(int)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
