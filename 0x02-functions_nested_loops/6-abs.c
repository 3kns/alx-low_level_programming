#include "main.h"
/**
 * _abs - entry point
 * @n: type of value to be returned
 * Return: absolute value (success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
